# Per-machine merged CSV — regenerated by scripts/merge_data.py from
# session_data/. The canonical pre-merge data is the JSONs in session_data/
# and the per-session CSVs in sessions_csv/, both of which ARE tracked.
data/.lock
__pycache__/
*.pyc
.venv/
venv/
**/.DS_Store
